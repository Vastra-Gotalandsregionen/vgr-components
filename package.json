{
  "name": "vgr-components",
  "private": true,
  "version": "0.0.0",
  "workspaces": ["packages/*", "apps/*"],
  "scripts": {
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "format:diff": "prettier --write --list-different ."
  },
  "devDependencies": {
    "@prettier/plugin-xml": "^3.4.2",
    "prettier": "3.9.6",
    "prettier-plugin-vue": "1.1.6"
  }
}
